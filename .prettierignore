dist
# @semantic-release/changelog writes to this file in a way whoch doesn't follow
# the rules.
CHANGELOG.md
# For some reason, running npm i changes the indentation style to tabs which
# makes npm run format fail.
package-lock.json
